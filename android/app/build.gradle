plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-plugin-loader"
}

android {
    namespace "com.apptools.systemsuite"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.apptools.systemsuite"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }
}

flutter {
    source '../..'
}
